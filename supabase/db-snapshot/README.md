# Current Supabase schema

Exactly one generated, schema-only export: the version-`0` baseline for the
current patch cycle. Truth model, baseline identity and methodology are in
`../README.md` — this artifact is deliberately not refreshed after patches.
