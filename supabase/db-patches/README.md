# Database patch ledger

Root-level SQL files (`<YYYYMMDD>-patch-<short-description>-<#>.sql`) are the
immutable, version-ordered ledger after the
canonical schema baseline (`../db-snapshot/`).

The only database-change methodology is documented in `../README.md`.
